extends layout

block content
  h1= title
  p Welcome to #{title}
  
  ul 
    each message, index in messages
      li #{index + 1}. 
        p <b>#{message.user}</b> said:
        p #{message.text}
        p <i>Posted: #{message.added.toDateString()}</i>

